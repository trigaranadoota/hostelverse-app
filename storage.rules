
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to review images.
    // This allows anyone to see the images attached to reviews.
    match /reviews/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
