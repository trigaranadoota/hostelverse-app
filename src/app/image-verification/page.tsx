import { ImageVerifier } from "@/components/onboarding/image-verifier";

export default function ImageVerificationPage() {
    return (
        <div>
            <ImageVerifier />
        </div>
    );
}
